# IT Wiki 重組最佳實踐

> **類型**: 最佳實踐  
> **日期**: 2026-01-03  
> **來源**: 對話 ID (2026-01-03 會話)

---

## 摘要

標準化 IT Wiki 目錄結構設計（編號 + 分類），包含文檔遷移、鏈接更新和索引自動生成，確保知識庫易於管理和查找。

---

## 背景

混亂的 Wiki 目錄結構會導致：

1. **查找困難**：文檔分類不清晰
2. **命名不一致**：有些用英文，有些用中文
3. **索引過時**：新增文檔後未更新 README.md
4. **鏈接失效**：路徑變更後內部鏈接失效

---

## 詳細內容

### 核心設計原則

#### 1. 編號 + 分類命名

使用兩位數字編號 + 英文分類名：

```
workspace/knowledge/
├── 02_architecture/        # 系統架構
├── 03_development/         # 開發指南
├── 04_frontend/            # 前端開發
├── 05_backend/             # 後端開發
├── 06_testing/             # 測試與 QA
├── 07_ai_llm/              # AI 與 LLM
├── 08_code_review/         # 代碼審查
├── 09_workflows/           # 工作流與流程
├── 10_insights/            # 經驗與反思
└── _templates/             # 文檔模板
```

**優點**：

- ✅ 編號確保排序一致
- ✅ 英文名稱避免編碼問題
- ✅ 分類清晰易於查找

#### 2. 文檔命名規範

| 文檔類型 | 命名格式             | 範例                           |
| -------- | -------------------- | ------------------------------ |
| 功能文檔 | `{功能名稱}.md`      | `07.1.api_strategy.md`              |
| 經驗總結 | `YYYYMMDD_{主題}.md` | `20260103_wiki_restructure.md` |
| 指南文檔 | `{主題}_guide.md`    | `01.1.setup_guide.md`               |

#### 3. README.md 自動生成

使用 `/run-wiki index` 自動生成索引：

```markdown
# Moyin Project Knowledge Base

> 最後更新：{YYYY-MM-DD HH:mm:ss}

---

## 📚 目錄結構

### 01. 快速開始 (Getting Started)

{分類描述}

- [{文件標題}]({相對路徑})
- [{文件標題}]({相對路徑})

---

## 📊 統計資訊

- **總文檔數**：{N}
- **分類數**：10
- **最後更新**：{YYYY-MM-DD HH:mm:ss}
```

#### 4. 文檔遷移流程

**方案 A：漸進式重組（推薦）**

- 保留現有目錄，新增編號前綴
- 逐步遷移舊文檔到新結構

**方案 B：一次性重組**

- 完全重建目錄結構
- 遷移所有現有文檔
- 更新所有內部鏈接

#### 5. 鏈接更新策略

使用相對路徑，避免絕對路徑：

❌ **錯誤示範**：

```markdown
[文檔](workspace/knowledge/architecture/02.1.protocol_design.md)
```

✅ **正確示範**：

```markdown
[文檔](02_architecture/02.1.protocol_design.md)
```

---

## 代碼/命令範例

### 範例 1：建立目錄結構（PowerShell）

```powershell
$base = "d:\AI-Novel-Projects\Moyin\workspace\knowledge"
$dirs = @(
    "02_architecture",
    "03_development",
    "04_frontend",
    "05_backend",
    "06_testing",
    "07_ai_llm",
    "08_code_review",
    "09_workflows",
    "10_insights",
    "_templates"
)

foreach ($dir in $dirs) {
    $path = Join-Path $base $dir
    if (-not (Test-Path $path)) {
        New-Item -Path $path -ItemType Directory -Force
    }
}
```

### 範例 2：文檔遷移映射表

```markdown
| 舊路徑                       | 新路徑                              | 操作          |
| ---------------------------- | ----------------------------------- | ------------- |
| `frontend-craft/ui_style.md` | `04_frontend/04.2.ui_design_system.md`   | 移動 + 重命名 |
| `vibecoding-guides/Rules.md` | `09_workflows/09.12.vibe_coding_rules.md` | 移動 + 重命名 |
```

### 範例 3：備份命令

```powershell
$timestamp = Get-Date -Format "yyyyMMdd_HHmmss"
Copy-Item -Path "workspace/knowledge" -Destination "workspace/knowledge_backup_$timestamp" -Recurse
```

---

## 適用場景

- **場景 1：新項目建立 Wiki**  
  直接使用標準化目錄結構，避免後期重組。

- **場景 2：現有 Wiki 重組**  
  先備份，再執行一次性重組或漸進式重組。

- **場景 3：多專案 Wiki 整合**  
  將多個專案的 Wiki 整合到統一結構。

- **場景 4：Wiki 維護**  
  定期執行 `/run-wiki index` 更新索引。

---

## 相關資源

- [run-wiki.md](../run_wiki.md) - Wiki 知識管理工作流
- [workspace/knowledge/](../../../workspace/knowledge/) - 知識庫目錄

---

## 注意事項

- ⚠️ **重組前必須備份**  
  使用時間戳命名備份目錄，範例：`knowledge_backup_20260103_162603`

- ⚠️ **文檔遷移後更新內部鏈接**  
  自動掃描所有 `.md` 文件，替換舊路徑為新路徑。

- ⚠️ **保留舊目錄 30 天**  
  軟刪除，避免外部鏈接立即失效。

- ⚠️ **索引自動生成**  
  不要手動編輯 README.md，使用 `/run-wiki index` 更新。

- ⚠️ **文檔模板統一放在 \_templates/**  
  使用下劃線前綴，確保排序在最後。

---

_本文檔由 `/run-wiki conversation` 自動生成_
